import { Properties } from '../../entities/Properties';
import { Property } from '../../../domain/property/property.entity';

export const toDomainProperty = (entity: Properties): Property => {
  return new Property(
    entity.propertyId,
    entity.locationId,
    entity.aoId,
    entity.blockId,
    entity.disseminationsId,
    entity.nbhId,
    entity.streetId,
    entity.subdivisionId,
    entity.consolidatedSubdivisionsId,
    entity.divisionId,
    entity.tractId,
    entity.metroId,
    entity.populationCentresId,
    entity.placeId,
    entity.provincesId,
    entity.lat,
    entity.lon,
    entity.addrStreetnum,
    entity.addrStrtPrefix,
    entity.addrStrtName,
    entity.addrStrtType,
    entity.addrStrtSuffix,
    entity.addrUnitNum,
    entity.addrUnitType,
    entity.addrCityname,
    entity.nbhName,
    entity.addrProvince,
    entity.addrPcode,
    entity.pcodeCity,
    entity.propType,
    entity.styleType,
    entity.beds,
    entity.bedsPlus,
    entity.baths,
    entity.partialBath,
    entity.sqftbuilding,
    entity.sqftlot,
    entity.yearbuilt,
    entity.rooms,
    entity.units,
    entity.stories,
    entity.garage,
    entity.garageType,
    entity.constType,
    entity.basement,
    entity.exteriorWall,
    entity.foundation,
    entity.roofCover,
    entity.ac,
    entity.heating,
    entity.fireplace,
    entity.fireplaceFeatures,
    entity.utilitySewer,
    entity.fencing,
    entity.features,
    entity.estPrice,
    entity.epHigh,
    entity.epLow,
    entity.epConf,
    entity.epDate,
    entity.estRent,
    entity.erHigh,
    entity.erLow,
    entity.erConf,
    entity.erDate,
    entity.prRatio,
    entity.lastListDate,
    entity.lastListPrice,
    entity.lastListStatus,
    entity.lastSalePrice,
    entity.lastSaleDate,
    entity.currentPremoverScore,
    entity.insertDt,
    entity.updateDt,
    entity.addressSlug,
  );
};
